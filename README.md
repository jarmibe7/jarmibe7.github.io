# Jared Berry | Robotics Portfolio

Static portfolio site for Jared Berry migrated from Jekyll. Open `index.html` directly or serve the repository with VS Code Live Server.

The original Jekyll content remains in `_pages`, `_projects`, `_data`, and `_bibliography` while the static rebuild lives at the repository root.
